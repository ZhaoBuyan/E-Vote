PORT=3000
JWT_SECRET=your_super_secret_key_change_me
JWT_EXPIRES_IN=24h

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=e_vote

# DeepSeek API（可选，留空则使用 Mock 模式；Optional; if left empty, Mock mode will be used）
DEEPSEEK_API_KEY=your_deepseek_api_key_optional
